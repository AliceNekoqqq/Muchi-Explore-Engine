# v1.0.0 测试流程

建议全部在“新建的测试对话”中完成，不要用正式存档第一次试验。

## A. 加载测试

1. 上传仓库文件到 `main`，暂时不要创建 `v1.0.0` Tag。
2. 导入角色卡 v30.9。
3. 新建对话。
4. 点击酒馆助手按钮“现场探索”。
5. 开场地点为地下安全屋，应该能直接打开探索界面，而不是提示“引擎尚未加载”。

如果失败，打开浏览器开发者控制台，执行：

```js
window.MuchiExplore
```

正常应能看到含 `open / close / summary / version / locations` 的对象，且 `locations` 有 11 个地点。

## B. 三入口联动

分别测试：

- 酒馆助手按钮“现场探索”；
- 状态栏地图页“搜索当前地点”；
- 暮迟地图 v2.5.4 当前地点的“现场探索”。

三个入口都应打开同一个探索界面，不应出现两个叠加实例。

## C. 防重 Roll

1. 对任意开放子区域执行一次“快速翻找”。
2. 记下发现物。
3. 关闭探索界面再打开。
4. 同一子区域的“快速翻找”应变成不可再次执行，发现物不应重新抽取。
5. 刷新酒馆页面后重新打开同一聊天，状态也应保留。

## D. 待收取与库存

1. 搜索得到物资后先不要收取。
2. 关闭并重开探索界面，应仍显示“待收取”。
3. 点击“收进你的背包”。
4. 状态栏物资页应出现对应物品；同一待收取批次不能第二次领取。

## E. 时间推进

搜索前记下世界时间和沈挽昼“抑制剩余分钟”。执行搜索后：

- 世界时间应立即推进；
- 抑制剩余分钟同步减少；
- 不需要等正文 AI 回复才发生。

## F. 正文桥接

1. 搜索后点击“写入剧情输入框”。
2. 输入框应出现 `[探索已结算]`。
3. 发送一次正文生成。
4. AI 可以描写过程，但不应再次追加随机掉落，也不应再次扣相同时间/资源/库存。

## G. 地图限制

打开暮迟地图：

- 当前所在地可以出现“现场探索”；
- 其他地点不允许远程探索；
- 机场、大学城、北山等仅区域情报地点不应出现可直接探索按钮。

## H. S11 专项（建议单独测试存档）

到槐安公寓后，确认哥哥房间/抽屉的既有开启条件与探索引擎一致；只有对应条件满足并达到规定搜索深度时才发现 S11，且 S11 不可重复生成。

## I. 测试通过后

GitHub 创建 Tag：

`v1.0.0`

之后角色卡会优先使用固定 Tag；`main` 继续作为开发/热修回退源。
