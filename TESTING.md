# v1.1.0 测试清单

1. 在稳定状态下打开探索：应显示行动余量 3/3。
2. 连点三个不同区域的“快速翻找”：第三次后本轮应锁定，第四个区域不能继续搜。
3. 关闭再打开探索界面：行动点不得恢复。
4. 未搜索区域直接“彻底搜索”：一次消耗3点并立即结束本轮。
5. 先快速再仔细：第二次只应再消耗1点，不重复计算快速层。
6. 本轮锁定后点击“写入本轮结果，返回剧情”，输入框应出现 `[探索行动轮已结算]`。
7. 仅关闭界面、刷新页面：仍应保持锁定。
8. 发送探索总结并得到下一条 AI 回复后，再次打开探索：应开始新一轮。
9. 将沈挽昼改为“发作前兆”或抑制剩余<=60：新一轮应只有2点行动窗口。
10. 将沈挽昼改为“半狂暴”：搜索按钮应被阻断并提示先处理状态。
11. 待收取物资在本轮锁定后仍可收取，但收取不能恢复行动点。
12. 地图应能读取 summary 中的 `roundState/roundRemaining`，当前地点显示待推进或剩余行动点。
